config:
  enabled: true
  clients:
    - url: http://loki-gateway.observability.svc.cluster.local/loki/api/v1/push

serviceMonitor:
  enabled: true

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi
